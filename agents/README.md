# Agents

В этой папке будут храниться runbook-файлы и инструкции для автоматических агентов проекта.

Текущий приоритет:

1. `SPY` EOD loader.
2. Расчёт греков локально через `py-vollib-vectorized`.
3. Позже отдельные runbook-и для M1, tick data, сверки backtest-брокер и обслуживания данных.
